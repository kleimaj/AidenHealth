import { Meta, Story, Preview } from "@storybook/addon-docs/blocks";
import { withKnobs, boolean } from "@storybook/addon-knobs";
import { SignUpModal, SignInModal } from "../components";

<Meta title="Components/Modals" component={SignUpModal} />

# Modals

Modals are dialogs which overlay content to convey. 

## Usage

Use modals to call the user to action or complete a task

## Sign Up Modal

The SignUpModal is used to prompt the user to create an account

<Preview>
  <Story name="signUp">
    <SignUpModal showModal={boolean("Show Modal", true)} storyBook={true} modifiers="storybookView" />
  </Story>
</Preview>

## Sign In Modal

The SignInModal is used to prompt the user to sign in or create an account. This modal contains a form field for users to input data.

<Story name="signIn">
  <SignInModal showModal={boolean("Show Modal", true)} storyBook={true} modifiers="storybookView" />
</Story>